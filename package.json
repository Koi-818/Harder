{
  "name": "har-record",
  "version": "2.1.0",
  "description": "使用 Playwright 录制浏览器 HAR 文件",
  "main": "src/cli.js",
  "scripts": {
    "test": "playwright test",
    "test:debug": "playwright test --debug",
    "start": "node src/cli.js",
    "dev": "node src/cli.js",
    "record": "node src/cli.js",
    "clean:logs": "node -e \"const fs=require('fs');const path=require('path');const dir=path.join(process.cwd(),'logs');if(fs.existsSync(dir)){fs.readdirSync(dir).forEach(f=>fs.unlinkSync(path.join(dir,f)));console.log('日志已清理');}else{console.log('日志目录不存在');}\""
  },
  "keywords": [
    "playwright",
    "har",
    "performance",
    "recording"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@types/node": "^25.1.0"
  }
}
